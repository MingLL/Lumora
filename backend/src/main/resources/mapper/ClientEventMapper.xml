<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.minglli.lumora.event.ClientEventMapper">
    <insert id="insert" parameterType="cn.minglli.lumora.event.ClientEventRecord">
        INSERT INTO client_event (visit_id, type, url, properties)
        VALUES (#{visitId}, #{type}, #{url}, CAST(#{propertiesJson} AS JSONB))
    </insert>
</mapper>
