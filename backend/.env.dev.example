# 微信公众号本地联调环境变量。
#
# 用法：
#   1. 去 https://mp.weixin.qq.com/debug/cgi-bin/sandbox 申请「测试号」，
#      拿到 appID 和 appSecret，并在「接口配置信息」里先随便填个 URL（等隧道起来再改）。
#   2. 复制本文件为 .env，填入测试号值：
#        cp .env.dev.example .env
#   3. 起本地栈（只跑回调用的 web，不跑会发邮件的 worker）：
#        docker compose --profile migrate up migrate
#        docker compose up -d web
#   4. 另开终端起公网隧道，拿到 https://xxx.trycloudflare.com：
#        ./scripts/dev-wechat-tunnel.sh
#   5. 回测试号后台把 URL 改成 {隧道地址}/wechat/callback/{WECHAT_APP_ID}，
#      Token 填 WECHAT_TOKEN，安全模式再填 WECHAT_AES_KEY。
#
# 这是开发联调用，绝不要用于生产。所有 *_ENABLED 在 compose 的 web 服务里
# 已被强制关掉，所以本地实例不会发邮件、不会跑定时任务。

# ---- 微信测试号（测试号后台给出）----
# appID
WECHAT_APP_ID=
# 测试号的原始 ID，形如 gh_xxxxxxx。推送里的 ToUserName 要和它一致。
# 测试号后台首页「微信号」一栏，或首次推送的 XML 里能看到。
WECHAT_ORIGINAL_ID=
# 自己定，和测试号后台「接口配置信息」里填的一致。
WECHAT_TOKEN=devtoken
# 自己定（43 位 Base64），用于安全模式。明文模式可留空但应用要求非空，
# 联调时若用明文模式随便填一个合法长度的占位串即可。
WECHAT_AES_KEY=abcdefghijklmnopqrstuvwxyz0123456789ABCDEFG

# ---- MySQL（compose 栈内服务名）----
MYSQL_HOST=mysql
MYSQL_PORT=3306
MYSQL_DATABASE=lumora
MYSQL_USERNAME=lumora
MYSQL_PASSWORD=lumora

# ---- 邮件（联调一般不真发；web 服务已关 internal-send）----
# 想测真实发信再填真实 QQ 邮箱授权码。
MAIL_USERNAME=dev@example.com
MAIL_AUTH_CODE=dev-auth-code
MAIL_FROM_NAME=Lumora-Dev
REPORT_RECIPIENTS=dev@example.com
REPORT_ADMIN_KEY=dev-admin-key

# ---- 区域 ----
LUMORA_ZONE=Asia/Shanghai

# ---- 以下被 compose 的 web 服务覆盖为 false，这里仅占位 ----
SCHEDULING_ENABLED=false
REPORT_RECOVERY_ENABLED=false
RETENTION_ENABLED=false
INTERNAL_SEND_ENABLED=false
WORKER_READY_MARKER=/tmp/lumora-worker-ready
