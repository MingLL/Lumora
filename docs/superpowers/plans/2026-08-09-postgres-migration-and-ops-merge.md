# PostgreSQL 迁移 + ops 合并 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把后端从 MySQL 8.4 切到 PostgreSQL 并把 `ops` 角色合并进 `worker`，让 dev2 从「3 个 JVM + MySQL」降到「2 个 JVM + PostgreSQL」，消除整机 OOM。

**Architecture:** 一次性硬切，不保留回滚路径（用户明确要求）。PostgreSQL 以 StatefulSet + hostPath 跑在 dev2，与 MySQL 并存到数据迁完为止。Flyway 的 V1/V2 直接改写为 PG 方言 —— 新库没有 `flyway_schema_history`，校验和不会冲突；旧 MySQL 库被废弃。`ops` 的合并需要改 `WorkerReadinessVerifier`，不是单纯改配置。

**Tech Stack:** Java 17 / Spring Boot 3.3 / MyBatis 3.0.4 / Flyway / PostgreSQL 17 / Testcontainers 1.21.4 / k3s

---

## 背景与既有约束

**为什么做**：dev2 物理内存 1870 Mi，跑 3 个 JVM（178+178+216 Mi）+ MySQL（484 Mi）+ k3s agent，只剩不到 400 Mi 缓冲。2026-08-08 15:00 和 08-09 15:44 发生过两次**整机级** OOM（被杀进程 RSS 均远低于各自 cgroup limit，证明不是限额触发）。MySQL 为 0.3 MB 的数据占用 484 Mi，主因是 `performance_schema=ON` 等独占服务器默认值。

**已量得的现状**：

| 项 | 值 |
|---|---|
| 业务数据 | 0.3 MB，5 张表（3 业务表 + flyway_schema_history + 1） |
| mapper 总行数 | 494 行，MySQL 方言仅 1 处 |
| 反引号标识符 | 0 处 |
| MySQL 数据目录 | `dev2:/opt/lumora/mysql`（210 MB，含 binlog） |

**用户已拍板的决定**：
1. 完整迁移历史数据（不空库重开）
2. 可接受几分钟停机
3. **不设计回滚**，只向前切

**主动放弃的设计边界**：`WorkerReadinessVerifier` 现有注释写明 *"Internal sending stays with the operations container so a worker cannot be driven from outside."* 合并后调度实例将可从集群内驱动。Ingress 只放行 `/wechat/callback`，公网仍打不到 `/internal/`，契约测试第「公网面」节继续锁这一点。

---

## 文件结构

| 文件 | 动作 | 职责 |
|---|---|---|
| `backend/pom.xml` | 改 | JDBC / Flyway / Testcontainers 依赖换 PG |
| `backend/src/main/resources/db/migration/V1__create_event_and_report_tables.sql` | 重写 | PG 方言建表 + `updated_at` 触发器 |
| `backend/src/main/resources/db/migration/V2__index_received_at_and_widen_raw_event_key.sql` | 重写 | PG 方言加索引 + 扩列 |
| `backend/src/main/resources/application.yml` | 改 | 数据源 URL / driver / 会话时区 |
| `backend/src/main/java/cn/minglli/lumora/config/LumoraProperties.java` | 改 | `mysql*` → `postgres*` |
| `backend/src/main/resources/mapper/ReportDeliveryMapper.xml` | 改 | `ON DUPLICATE KEY UPDATE` → `ON CONFLICT DO NOTHING` |
| `backend/src/main/java/cn/minglli/lumora/operations/WorkerReadinessVerifier.java` | 改 | 允许 worker 同时开内部发送 |
| `backend/src/test/java/cn/minglli/lumora/support/MySqlContainerTest.java` | 删 | 由下一行取代 |
| `backend/src/test/java/cn/minglli/lumora/support/PostgresContainerTest.java` | 建 | Testcontainers 基类换 PG |
| `backend/.env.example` / `backend/deploy/check-env.sh` | 改 | 环境变量改名 |
| `deploy/k8s/lumora-mysql.yaml` | 删 | 由下一行取代 |
| `deploy/k8s/lumora-postgres.yaml` | 建 | PG StatefulSet + Service |
| `deploy/k8s/lumora-backend.yaml` | 改 | 删 ops Deployment + Service，worker 开内部发送 |
| `deploy/deploy-backend.sh` | 改 | rollout 列表去掉 ops |
| `deploy/tests/deploy_contract_test.sh` | 改 | 角色断言 3→2、Service Links 4→3 |
| `deploy/migrate-mysql-to-postgres.py` | 建 | 一次性数据搬运 + 序列重置 |
| `deploy/README.md` / `README.md` / `backend/README.md` | 改 | 文档同步 |

---

### Task 1: 依赖换成 PostgreSQL

**Files:**
- Modify: `backend/pom.xml:62-92`

- [ ] **Step 1: 替换三处依赖**

把 `flyway-mysql` 换成 `flyway-database-postgresql`：

```xml
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>
```

把 `com.mysql:mysql-connector-j` 换成：

```xml
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
```

把 testcontainers 的 `mysql` 换成：

```xml
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
            <scope>test</scope>
        </dependency>
```

- [ ] **Step 2: 确认依赖树里再没有 mysql**

Run: `cd backend && mvn -q dependency:tree | grep -i mysql`
Expected: 无输出（退出码 1 即可）

- [ ] **Step 3: Commit**

```bash
git add backend/pom.xml
git commit -m "build: swap MySQL driver and Flyway module for PostgreSQL"
```

---

### Task 2: 重写 V1 建表脚本为 PG 方言

**Files:**
- Modify: `backend/src/main/resources/db/migration/V1__create_event_and_report_tables.sql`

**为什么直接改而不是新增 V3**：目标 PG 库是全新的，没有 `flyway_schema_history`，不存在校验和冲突；而 V1 现有内容是纯 MySQL 语法，在 PG 上根本执行不了，无法「增量修补」。

- [ ] **Step 1: 整体替换文件内容**

```sql
-- PostgreSQL 方言。原 MySQL 版本已废弃：本项目 2026-08-09 从 MySQL 8.4 硬切到
-- PostgreSQL，目标库是全新的，没有 flyway_schema_history，因此直接改写而非新增迁移。
--
-- 与 MySQL 版的三处语义差异，改动时注意：
--   1. id 用 GENERATED BY DEFAULT AS IDENTITY 而非 ALWAYS —— 数据迁移要显式插入
--      历史主键，ALWAYS 需要 OVERRIDING SYSTEM VALUE，容易漏写。
--   2. JSON → JSONB。MyBatis 取值仍按字符串处理，读写不受影响。
--   3. MySQL 的 ON UPDATE CURRENT_TIMESTAMP 在 PG 没有等价物，靠下面的触发器补。
--      差别：MySQL 仅在行内容确实变化时更新该列，PG 触发器每次 UPDATE 都会更新。

CREATE TABLE wechat_event (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    app_id VARCHAR(64) NOT NULL,
    open_id VARCHAR(128) NOT NULL,
    event_type VARCHAR(32) NOT NULL,
    raw_msg_type VARCHAR(32) NOT NULL,
    raw_event VARCHAR(64) NULL,
    message_id BIGINT NULL,
    original_occurred_at TIMESTAMP(6) NOT NULL,
    effective_occurred_at TIMESTAMP(6) NOT NULL,
    received_at TIMESTAMP(6) NOT NULL,
    anomalous_timestamp BOOLEAN NOT NULL DEFAULT FALSE,
    deduplication_key VARCHAR(71) NOT NULL,
    raw_event_key VARCHAR(512) NULL,
    qr_scene VARCHAR(512) NULL,
    ticket VARCHAR(512) NULL,
    ticket_present BOOLEAN NOT NULL DEFAULT FALSE,
    menu_key VARCHAR(512) NULL,
    menu_url VARCHAR(2048) NULL,
    latitude DECIMAL(10, 7) NULL,
    longitude DECIMAL(10, 7) NULL,
    location_precision DECIMAL(12, 6) NULL,
    composite_type VARCHAR(32) NULL,
    composite_item_count INT NULL,
    composite_sha256 CHAR(64) NULL,
    safe_summary JSONB NOT NULL,
    normalized_message_sha256 CHAR(64) NOT NULL,
    created_at TIMESTAMP(6) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT uq_event_dedup UNIQUE (app_id, deduplication_key)
);

CREATE INDEX ix_event_report ON wechat_event (effective_occurred_at, event_type);
CREATE INDEX ix_event_user ON wechat_event (open_id, effective_occurred_at);

CREATE TABLE daily_report (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    report_date DATE NOT NULL,
    version INT NOT NULL,
    window_start TIMESTAMP(6) NOT NULL,
    window_end TIMESTAMP(6) NOT NULL,
    data_cutoff_at TIMESTAMP(6) NOT NULL,
    snapshot_json JSONB NOT NULL,
    created_at TIMESTAMP(6) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT uq_report_version UNIQUE (report_date, version)
);

CREATE TABLE report_delivery_attempt (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    delivery_id CHAR(36) NOT NULL,
    report_id BIGINT NOT NULL,
    trigger_type VARCHAR(16) NOT NULL,
    request_id VARCHAR(128) NULL,
    -- 生成列：只有 AUTO 触发的行参与 uq_auto_report 唯一约束，MANUAL 行取 NULL。
    -- PG 与 MySQL 一致，唯一约束中多个 NULL 互不冲突。
    auto_report_id BIGINT GENERATED ALWAYS AS (
        CASE WHEN trigger_type = 'AUTO' THEN report_id ELSE NULL END
    ) STORED,
    status VARCHAR(16) NOT NULL,
    recipient_masked VARCHAR(1024) NOT NULL,
    recipient_sha256 CHAR(64) NOT NULL,
    attempt_count INT NOT NULL DEFAULT 0,
    claimed_at TIMESTAMP(6) NULL,
    lease_until TIMESTAMP(6) NULL,
    completed_at TIMESTAMP(6) NULL,
    last_error_class VARCHAR(128) NULL,
    last_error_summary VARCHAR(500) NULL,
    created_at TIMESTAMP(6) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP(6) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT uq_delivery_id UNIQUE (delivery_id),
    CONSTRAINT uq_auto_report UNIQUE (auto_report_id),
    CONSTRAINT uq_manual_request UNIQUE (report_id, request_id),
    CONSTRAINT fk_delivery_report
        FOREIGN KEY (report_id) REFERENCES daily_report (id)
);

-- 替代 MySQL 的 ON UPDATE CURRENT_TIMESTAMP(6)。没有它，updated_at 会在第一次
-- 写入后永远停在创建时间 —— 不报错，只是数据悄悄变错，租约回收会读到错误的时间。
CREATE OR REPLACE FUNCTION lumora_set_updated_at() RETURNS TRIGGER AS $$
BEGIN
    NEW.updated_at = CURRENT_TIMESTAMP;
    RETURN NEW;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER trg_delivery_updated_at
    BEFORE UPDATE ON report_delivery_attempt
    FOR EACH ROW EXECUTE FUNCTION lumora_set_updated_at();
```

- [ ] **Step 2: Commit**

```bash
git add backend/src/main/resources/db/migration/V1__create_event_and_report_tables.sql
git commit -m "feat(db): rewrite V1 schema in PostgreSQL dialect with updated_at trigger"
```

---

### Task 3: 重写 V2 为 PG 方言

**Files:**
- Modify: `backend/src/main/resources/db/migration/V2__index_received_at_and_widen_raw_event_key.sql`

- [ ] **Step 1: 整体替换文件内容**

```sql
-- PostgreSQL 方言，随 V1 一同改写（见该文件头部说明）。保留为独立迁移而不是并入
-- V1，是为了让 schema 的演进历史继续可读：这两条改动当初各有各的线上事故背景。

-- Retention filters and deletes by received_at on every run; V1 only indexed
-- effective_occurred_at and open_id, so those statements were full table scans.
CREATE INDEX ix_event_received_at ON wechat_event (received_at);

-- raw_event_key holds the same value as menu_url for VIEW events, where WeChat
-- allows a link of up to 1024 bytes. At VARCHAR(512) a long menu link made the
-- insert fail, which surfaced as a 503 and an endless WeChat retry loop.
ALTER TABLE wechat_event ALTER COLUMN raw_event_key TYPE VARCHAR(2048);
```

- [ ] **Step 2: Commit**

```bash
git add backend/src/main/resources/db/migration/V2__index_received_at_and_widen_raw_event_key.sql
git commit -m "feat(db): rewrite V2 migration in PostgreSQL dialect"
```

---

### Task 4: 数据源配置与属性改名

**Files:**
- Modify: `backend/src/main/resources/application.yml:4-9` 和 `lumora:` 段
- Modify: `backend/src/main/java/cn/minglli/lumora/config/LumoraProperties.java:36-51,124-161`

- [ ] **Step 1: 改 application.yml 的数据源**

```yaml
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST}:${POSTGRES_PORT:5432}/${POSTGRES_DATABASE}
    username: ${POSTGRES_USERNAME}
    password: ${POSTGRES_PASSWORD}
    driver-class-name: org.postgresql.Driver
    hikari:
      # 取代 MySQL URL 上的 connectionTimeZone=UTC&forceConnectionTimeZoneToSession=true。
      # 应用统一按 UTC 存取，会话时区必须钉死，否则容器时区一变读出来的时间就偏。
      connection-init-sql: SET TIME ZONE 'UTC'
```

- [ ] **Step 2: 改 application.yml 的 lumora 段**

把这五行：

```yaml
  mysql-host: ${MYSQL_HOST:}
  mysql-port: ${MYSQL_PORT:3306}
  mysql-database: ${MYSQL_DATABASE:}
  mysql-username: ${MYSQL_USERNAME:}
  mysql-password: ${MYSQL_PASSWORD:}
```

换成：

```yaml
  postgres-host: ${POSTGRES_HOST:}
  postgres-port: ${POSTGRES_PORT:5432}
  postgres-database: ${POSTGRES_DATABASE:}
  postgres-username: ${POSTGRES_USERNAME:}
  postgres-password: ${POSTGRES_PASSWORD:}
```

- [ ] **Step 3: 改 LumoraProperties 字段与访问器**

字段段（原 36-51 行）改为：

```java
    @NotBlank(message = "POSTGRES_HOST must not be blank")
    private String postgresHost;

    @NotNull(message = "POSTGRES_PORT must be provided")
    @Min(value = 1, message = "POSTGRES_PORT must be between 1 and 65535")
    @Max(value = 65535, message = "POSTGRES_PORT must be between 1 and 65535")
    private Integer postgresPort;

    @NotBlank(message = "POSTGRES_DATABASE must not be blank")
    private String postgresDatabase;

    @NotBlank(message = "POSTGRES_USERNAME must not be blank")
    private String postgresUsername;

    @NotBlank(message = "POSTGRES_PASSWORD must not be blank")
    private String postgresPassword;
```

访问器段（原 124-161 行）改为：

```java
    public String getPostgresHost() {
        return postgresHost;
    }

    public void setPostgresHost(String postgresHost) {
        this.postgresHost = postgresHost;
    }

    public Integer getPostgresPort() {
        return postgresPort;
    }

    public void setPostgresPort(Integer postgresPort) {
        this.postgresPort = postgresPort;
    }

    public String getPostgresDatabase() {
        return postgresDatabase;
    }

    public void setPostgresDatabase(String postgresDatabase) {
        this.postgresDatabase = postgresDatabase;
    }

    public String getPostgresUsername() {
        return postgresUsername;
    }

    public void setPostgresUsername(String postgresUsername) {
        this.postgresUsername = postgresUsername;
    }

    public String getPostgresPassword() {
        return postgresPassword;
    }

    public void setPostgresPassword(String postgresPassword) {
        this.postgresPassword = postgresPassword;
    }
```

- [ ] **Step 4: 全仓搜残留引用**

Run: `grep -rn "mysql\|MYSQL" backend/src/main --include=*.java --include=*.yml --include=*.xml`
Expected: 无输出

- [ ] **Step 5: 编译**

Run: `cd backend && mvn -q -DskipTests compile`
Expected: BUILD SUCCESS（`LumoraPropertiesTest` 此时可能仍引用旧名，Task 6 修）

- [ ] **Step 6: Commit**

```bash
git add backend/src/main/resources/application.yml backend/src/main/java/cn/minglli/lumora/config/LumoraProperties.java
git commit -m "feat(config): point datasource at PostgreSQL and rename MYSQL_* to POSTGRES_*"
```

---

### Task 5: mapper 的唯一一处方言

**Files:**
- Modify: `backend/src/main/resources/mapper/ReportDeliveryMapper.xml:33-42`

`upsertAuto` 的语义是「已存在就什么都不做」。MySQL 用 `ON DUPLICATE KEY UPDATE id = id` 表达空更新；PG 直接有 `DO NOTHING`。调用方（`ReportDeliveryService:73-75`）忽略返回值、随后另做一次 `findAutoByReportId`，所以受影响行数从 1 变 0 不影响行为。

- [ ] **Step 1: 改写 upsertAuto**

```xml
    <insert id="upsertAuto">
        INSERT INTO report_delivery_attempt (
            delivery_id, report_id, trigger_type, status,
            recipient_masked, recipient_sha256
        ) VALUES (
            #{deliveryId}, #{reportId}, 'AUTO', 'PENDING',
            #{recipientMasked}, #{recipientSha256}
        )
        ON CONFLICT DO NOTHING
    </insert>
```

- [ ] **Step 2: 确认全仓再无 MySQL 方言**

Run: `grep -rniE "ON DUPLICATE KEY|INSERT IGNORE|REPLACE INTO|IFNULL|DATE_FORMAT|LAST_INSERT_ID" backend/src/main/resources/mapper/`
Expected: 无输出

- [ ] **Step 3: Commit**

```bash
git add backend/src/main/resources/mapper/ReportDeliveryMapper.xml
git commit -m "feat(db): replace MySQL upsert with PostgreSQL ON CONFLICT DO NOTHING"
```

---

### Task 6: 测试基类换 PostgreSQL

**Files:**
- Delete: `backend/src/test/java/cn/minglli/lumora/support/MySqlContainerTest.java`
- Create: `backend/src/test/java/cn/minglli/lumora/support/PostgresContainerTest.java`
- Modify: 所有 `extends MySqlContainerTest` 的测试类

- [ ] **Step 1: 建新基类**

```java
package cn.minglli.lumora.support;

import org.flywaydb.core.Flyway;
import org.junit.jupiter.api.BeforeAll;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.DynamicPropertyRegistry;
import org.springframework.test.context.DynamicPropertySource;
import org.testcontainers.containers.PostgreSQLContainer;

@SpringBootTest(properties = {
        "lumora.wechat-app-id=wx-app-id",
        "lumora.wechat-original-id=gh_original",
        "lumora.wechat-token=wechat-token",
        "lumora.wechat-aes-key=abcdefghijklmnopqrstuvwxyz0123456789ABCDEFG",
        "lumora.wechat-app-secret=wechat-app-secret",
        "lumora.postgres-host=localhost",
        "lumora.postgres-port=5432",
        "lumora.postgres-database=lumora",
        "lumora.postgres-username=lumora",
        "lumora.postgres-password=lumora",
        "lumora.mail-username=sender@qq.com",
        "lumora.mail-auth-code=mail-auth-code",
        "lumora.report-recipients=owner@example.com",
        "lumora.report-admin-key=admin-secret",
        "spring.flyway.enabled=false"
})
public abstract class PostgresContainerTest {

    /**
     * Singleton container, started once for the whole JVM and never stopped here.
     *
     * <p>Deliberately not {@code @Testcontainers}/{@code @Container}: that pair stops
     * the container in each subclass's {@code afterAll}, while Spring's context cache
     * survives across test classes. The second integration class to run would then
     * reuse a cached datasource pointing at a container that no longer exists, and
     * fail with "Connection refused" 30 seconds at a time. Ryuk removes the container
     * when the JVM exits.
     */
    protected static final PostgreSQLContainer<?> POSTGRES = new PostgreSQLContainer<>("postgres:17-alpine")
            .withDatabaseName("lumora")
            .withUsername("lumora")
            .withPassword("lumora");

    static {
        POSTGRES.start();
    }

    @DynamicPropertySource
    static void postgresProperties(DynamicPropertyRegistry registry) {
        registry.add("spring.datasource.url", POSTGRES::getJdbcUrl);
        registry.add("spring.datasource.username", POSTGRES::getUsername);
        registry.add("spring.datasource.password", POSTGRES::getPassword);
        registry.add("spring.datasource.hikari.connection-init-sql", () -> "SET TIME ZONE 'UTC'");
    }

    @BeforeAll
    static void migrateDatabase() {
        Flyway.configure()
                .dataSource(POSTGRES.getJdbcUrl(), POSTGRES.getUsername(), POSTGRES.getPassword())
                .locations("classpath:db/migration")
                .load()
                .migrate();
    }
}
```

- [ ] **Step 2: 删旧基类并改所有引用**

```bash
rm backend/src/test/java/cn/minglli/lumora/support/MySqlContainerTest.java
grep -rl "MySqlContainerTest" backend/src/test | xargs sed -i '' 's/MySqlContainerTest/PostgresContainerTest/g'
```

- [ ] **Step 3: 改 LumoraPropertiesTest 里的属性名**

```bash
grep -rl "mysql-" backend/src/test | xargs sed -i '' \
  -e 's/lumora\.mysql-host/lumora.postgres-host/g' \
  -e 's/lumora\.mysql-port=3306/lumora.postgres-port=5432/g' \
  -e 's/lumora\.mysql-database/lumora.postgres-database/g' \
  -e 's/lumora\.mysql-username/lumora.postgres-username/g' \
  -e 's/lumora\.mysql-password/lumora.postgres-password/g'
grep -rn "MYSQL_\|mysql" backend/src/test || echo "clean"
```

Expected: `clean`（若仍有命中，逐个手工改；`LumoraPropertiesTest` 里断言的错误消息文本也从 `MYSQL_` 改成 `POSTGRES_`）

- [ ] **Step 4: 跑全量测试（需要 Docker）**

Run: `cd backend && mvn test`
Expected: BUILD SUCCESS，Testcontainers 拉起 `postgres:17-alpine`

- [ ] **Step 5: Commit**

```bash
git add -A backend/src/test
git commit -m "test: run integration tests against PostgreSQL Testcontainers"
```

---

### Task 7: 允许 worker 兼任内部发送

**Files:**
- Modify: `backend/src/main/java/cn/minglli/lumora/operations/WorkerReadinessVerifier.java:78-87`

现有实现要求 `internalSendEnabled == false` 才认为是 worker。合并 ops 后 worker 必须同时开这个开关，不改这里就绪标记永远写不出来，滚动更新会卡在探针失败。

- [ ] **Step 1: 改判定与注释**

```java
    /**
     * A worker owns all background work. Since 2026-08-09 it also serves the internal
     * send endpoint: the separate ops container was merged away to free ~180 MiB on
     * dev2, which was hitting node-level OOM. Internal sending is therefore no longer
     * a disqualifier here. The endpoint stays unreachable from the internet because
     * the Ingress only routes /wechat/callback; the contract test asserts that.
     */
    private boolean isWorkerMode() {
        return properties.isSchedulingEnabled()
                && properties.isReportRecoveryEnabled()
                && properties.isRetentionEnabled();
    }
```

- [ ] **Step 2: 确认没有别的地方依赖「worker 不开内部发送」**

Run: `grep -rn "isInternalSendEnabled" backend/src/main/java`
Expected: 只剩 `LumoraProperties`（定义）和 `ManualReportController:50`（端点门控）两处，以及 `WorkerReadinessVerifier:66` 的日志

- [ ] **Step 3: Commit**

```bash
git add backend/src/main/java/cn/minglli/lumora/operations/WorkerReadinessVerifier.java
git commit -m "feat(ops): let the worker serve internal sends so ops can be merged away"
```

---

### Task 8: PostgreSQL 的 k8s 清单

**Files:**
- Create: `deploy/k8s/lumora-postgres.yaml`
- Delete: `deploy/k8s/lumora-mysql.yaml`（在 Task 11 数据迁完后再删）

- [ ] **Step 1: 写清单**

```yaml
# 集群内的 PostgreSQL，取代原来的 MySQL 8.4。
#
#   - 数据落在 __PG_NODE__ 的 /opt/lumora/postgres，用 nodeSelector 钉死在那台，
#     因为 hostPath 不跟着 pod 漂移，换节点就等于换了一个空库。
#   - 线上当前钉在 dev2。备份要认准这台。
#   - 备份和加密是你的责任，见 backend/README.md 的「Operating the Database」。
#     hostPath 本身不提供任何加密。
#
# 为什么从 MySQL 换过来：MySQL 8.4 为 0.3 MB 的数据常驻 484 MiB（performance_schema
# 等独占服务器默认值），在 1870 MiB 的 dev2 上和三个 JVM 一起把节点推到 OOM。
---
apiVersion: v1
kind: Service
metadata:
  name: lumora-postgres
  namespace: lumora
spec:
  clusterIP: None
  selector:
    app: lumora-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: lumora-postgres
  namespace: lumora
  labels:
    app: lumora-postgres
spec:
  serviceName: lumora-postgres
  replicas: 1
  selector:
    matchLabels:
      app: lumora-postgres
  template:
    metadata:
      labels:
        app: lumora-postgres
    spec:
      nodeSelector:
        kubernetes.io/hostname: __PG_NODE__
      containers:
        - name: postgres
          # 和前端 nginx 同样的理由走 ECR Public：阿里云机器直连 Docker Hub 不通。
          image: public.ecr.aws/docker/library/postgres:17-alpine
          imagePullPolicy: IfNotPresent
          args:
            # 应用侧统一用 UTC 存取，库也钉在 UTC，避免跨时区偏移。
            - "-c"
            - "timezone=UTC"
            # 0.3 MB 的库，默认值是给独占服务器调的，全部收小。
            - "-c"
            - "shared_buffers=32MB"
            - "-c"
            - "max_connections=32"
            - "-c"
            - "work_mem=2MB"
            - "-c"
            - "maintenance_work_mem=16MB"
          env:
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: lumora-env
                  key: POSTGRES_DATABASE
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: lumora-env
                  key: POSTGRES_USERNAME
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: lumora-env
                  key: POSTGRES_PASSWORD
            # 官方镜像默认把数据放在 /var/lib/postgresql/data 的根，挂 hostPath 时
            # lost+found 之类的残留会让 initdb 拒绝启动，所以下沉一层子目录。
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          ports:
            - name: postgres
              containerPort: 5432
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          resources:
            requests:
              cpu: 100m
              memory: 192Mi
            limits:
              cpu: "1"
              memory: 384Mi
          readinessProbe:
            exec:
              command: ["pg_isready", "-h", "127.0.0.1", "-q"]
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            exec:
              command: ["pg_isready", "-h", "127.0.0.1", "-q"]
            initialDelaySeconds: 30
            periodSeconds: 30
          securityContext:
            allowPrivilegeEscalation: false
      volumes:
        - name: data
          hostPath:
            path: /opt/lumora/postgres
            type: DirectoryOrCreate
```

- [ ] **Step 2: 服务端 dry-run 校验**

```bash
sed 's/__PG_NODE__/dev2/g' deploy/k8s/lumora-postgres.yaml \
  | ssh dev1 "cat > /tmp/dry-pg.yaml && sudo /usr/local/bin/k3s kubectl apply --dry-run=server -f /tmp/dry-pg.yaml; rm -f /tmp/dry-pg.yaml"
```

Expected: `service/lumora-postgres created (server dry run)` 与 `statefulset.apps/lumora-postgres created (server dry run)`

- [ ] **Step 3: Commit**

```bash
git add deploy/k8s/lumora-postgres.yaml
git commit -m "feat(deploy): add PostgreSQL StatefulSet tuned for a 1.8 GiB node"
```

---

### Task 9: 删掉 ops 角色

**Files:**
- Modify: `deploy/k8s/lumora-backend.yaml`（删 ops Service 与 Deployment，改 worker 环境变量与内存注释）

- [ ] **Step 1: 删掉 ops 的 Service 与 Deployment**

删除 `# ops 只在集群内可达，故意不给 Ingress。` 起的那个 `Service`（`name: lumora-backend-ops`）整段，以及 `name: lumora-backend-ops` 的整个 `Deployment` 段（含其前后的 `---` 分隔符各一个，注意别把相邻资源的分隔符删掉）。

- [ ] **Step 2: 让 worker 打开内部发送**

在 `lumora-backend-worker` 的 `env` 里，把

```yaml
            - name: INTERNAL_SEND_ENABLED
              value: "false"
```

改成

```yaml
            # ops 容器已于 2026-08-09 合并进来，手动补发改在 worker 上执行。
            - name: INTERNAL_SEND_ENABLED
              value: "true"
```

- [ ] **Step 3: 更新文件头的内存预算注释**

把内存预算那段改为：

```
# 内存预算（两个 Deployment 加 PostgreSQL 全在 dev2，物理内存 1870 Mi）：
#
#   web / worker         requests 256Mi   limits 384Mi   ×2
#   lumora-postgres      requests 192Mi   limits 384Mi        （见 lumora-postgres.yaml）
#   ─────────────────────────────────────────────────────
#   合计                 requests 704Mi   limits 1152Mi
#
# 2026-08-08/09 dev2 发生过两次整机 OOM：被杀进程的 RSS 都远低于各自 limit，
# 说明不是 cgroup 限额触发，而是 3 个 JVM + MySQL 根本装不下。这一版把 ops 合并
# 进 worker（省一个 JVM）并用 PostgreSQL 取代 MySQL（484 Mi → 约 100 Mi），
# limits 合计从 1920 Mi 降到 1152 Mi，节点终于有了真正的余量。
```

- [ ] **Step 4: 服务端 dry-run**

```bash
sed -e 's|__IMAGE__|lumora-backend:dryrun|g' -e 's|__MANIFEST_HASH__|deadbeef|g' deploy/k8s/lumora-backend.yaml \
  | ssh dev1 "cat > /tmp/dry-be.yaml && sudo /usr/local/bin/k3s kubectl apply --dry-run=server -f /tmp/dry-be.yaml; rm -f /tmp/dry-be.yaml"
```

Expected: 只出现 web / worker 两个 Deployment，无 `lumora-backend-ops`

- [ ] **Step 5: Commit**

```bash
git add deploy/k8s/lumora-backend.yaml
git commit -m "feat(deploy): merge the ops role into worker to free a JVM on dev2"
```

---

### Task 10: 发布脚本与契约测试

**Files:**
- Modify: `deploy/deploy-backend.sh:138`、脚本末尾的补发提示
- Modify: `deploy/tests/deploy_contract_test.sh:199,247-249`

- [ ] **Step 1: rollout 列表去掉 ops**

```bash
for d in lumora-backend-web lumora-backend-worker; do
```

- [ ] **Step 2: 补发提示改指向 worker**

把脚本末尾 `printf '  ssh %s "sudo %s kubectl -n %s exec deployment/lumora-backend-ops \\\n"` 那一行里的 `lumora-backend-ops` 改成 `lumora-backend-worker`。

- [ ] **Step 3: 契约测试角色列表去掉 ops**

```bash
for role in web worker; do
```

- [ ] **Step 4: Service Links 计数 4 → 3**

```bash
[[ "$service_links_disabled" == "3" ]] \
  && ok "三种后端 Pod 都禁用 Kubernetes Service 环境变量注入" \
  || no "只有 ${service_links_disabled} 个后端 Pod 禁用 Service Links，预期为 3"
```

- [ ] **Step 5: 内存预算断言下调**

把 `mem_budget=1920` 改成 `mem_budget=1152`，并把 awk 读取的第二个文件从 `lumora-mysql.yaml` 改成 `lumora-postgres.yaml`：

```bash
mem_budget=1152
```

```bash
  ' "$TEST_ROOT/deploy/k8s/lumora-backend.yaml" "$TEST_ROOT/deploy/k8s/lumora-postgres.yaml"
```

- [ ] **Step 6: 跑契约测试**

Run: `bash deploy/tests/deploy_contract_test.sh`
Expected: `0 失败`

- [ ] **Step 7: Commit**

```bash
git add deploy/deploy-backend.sh deploy/tests/deploy_contract_test.sh
git commit -m "test(deploy): lock the two-role layout and the new memory budget"
```

---

### Task 11: 环境变量清单

**Files:**
- Modify: `backend/.env.example`
- Modify: `backend/deploy/check-env.sh:16-28`

- [ ] **Step 1: 改 .env.example**

把 `MYSQL_HOST=` / `MYSQL_PORT=3306` / `MYSQL_DATABASE=` / `MYSQL_USERNAME=` / `MYSQL_PASSWORD=` 五行换成：

```
POSTGRES_HOST=
POSTGRES_PORT=5432
POSTGRES_DATABASE=
POSTGRES_USERNAME=
POSTGRES_PASSWORD=
```

- [ ] **Step 2: 改 check-env.sh 的必需变量表**

把 `REQUIRED` 数组里的四个 `MYSQL_*` 换成 `POSTGRES_HOST` / `POSTGRES_DATABASE` / `POSTGRES_USERNAME` / `POSTGRES_PASSWORD`。

- [ ] **Step 3: 用模板自检**

Run: `bash backend/deploy/check-env.sh backend/.env.example`
Expected: `ok: backend/.env.example defines all 13 required variables`

- [ ] **Step 4: Commit**

```bash
git add backend/.env.example backend/deploy/check-env.sh
git commit -m "chore(deploy): rename MYSQL_* env vars to POSTGRES_*"
```

---

### Task 12: 数据搬运脚本

**Files:**
- Create: `deploy/migrate-mysql-to-postgres.py`

在本机跑，靠两条 `kubectl port-forward` 分别连上新旧库。0.3 MB 的数据量不需要流式处理，整表读进内存即可。

- [ ] **Step 1: 写脚本**

```python
#!/usr/bin/env python3
"""把 lumora 的业务数据从 MySQL 搬到 PostgreSQL。一次性脚本，2026-08-09 切库用。

前置：两条 port-forward 各自开着（见 deploy/README.md「切到 PostgreSQL」一节），
且 PG 上已经跑完 Flyway 迁移（表结构已就位，且是空的）。

    pip install pymysql psycopg2-binary
    python3 deploy/migrate-mysql-to-postgres.py

三件容易出错、这里显式处理掉的事：
  1. auto_report_id 是 PG 的生成列，不能出现在 INSERT 列名里，否则报错。
  2. MySQL 的 BOOLEAN 实为 TINYINT(1)，读出来是 0/1，PG 的 BOOLEAN 不接受整数。
  3. IDENTITY 序列不会因为显式插入主键而前进，搬完必须 setval，否则下一次
     插入就撞主键冲突。
"""
import os
import sys

import pymysql
import psycopg2
import psycopg2.extras

# 先父后子：report_delivery_attempt 有指向 daily_report 的外键。
TABLES = [
    ("wechat_event", ["anomalous_timestamp", "ticket_present"]),
    ("daily_report", []),
    ("report_delivery_attempt", []),
]
GENERATED_COLUMNS = {"auto_report_id"}


def env(name, default=None):
    value = os.environ.get(name, default)
    if value is None:
        sys.exit(f"missing env var: {name}")
    return value


def main():
    my = pymysql.connect(
        host=env("MY_HOST", "127.0.0.1"), port=int(env("MY_PORT", "3306")),
        user=env("MY_USER"), password=env("MY_PASSWORD"), database=env("MY_DATABASE"),
        cursorclass=pymysql.cursors.DictCursor,
    )
    pg = psycopg2.connect(
        host=env("PG_HOST", "127.0.0.1"), port=int(env("PG_PORT", "5432")),
        user=env("PG_USER"), password=env("PG_PASSWORD"), dbname=env("PG_DATABASE"),
    )
    pg.autocommit = False

    with my.cursor() as mc, pg.cursor() as pc:
        for table, bool_columns in TABLES:
            mc.execute(f"SELECT * FROM {table} ORDER BY id")
            rows = mc.fetchall()
            if not rows:
                print(f"{table}: 0 rows, skipped")
                continue

            columns = [c for c in rows[0].keys() if c not in GENERATED_COLUMNS]
            for row in rows:
                for column in bool_columns:
                    row[column] = bool(row[column])

            values = [tuple(row[c] for c in columns) for row in rows]
            placeholders = ",".join(["%s"] * len(columns))
            psycopg2.extras.execute_batch(
                pc,
                f'INSERT INTO {table} ({",".join(columns)}) VALUES ({placeholders})',
                values,
            )

            # 序列名由 PG 自动生成：<表名>_<列名>_seq。
            pc.execute(
                f"SELECT setval(pg_get_serial_sequence('{table}', 'id'), "
                f"(SELECT MAX(id) FROM {table}))"
            )
            print(f"{table}: {len(rows)} rows copied, sequence reset")

    pg.commit()

    # 校验：逐表比对行数，不一致就整体回滚已经来不及，所以只报错让人工介入。
    with my.cursor() as mc, pg.cursor() as pc:
        for table, _ in TABLES:
            mc.execute(f"SELECT COUNT(*) AS n FROM {table}")
            source = mc.fetchone()["n"]
            pc.execute(f"SELECT COUNT(*) FROM {table}")
            target = pc.fetchone()[0]
            status = "ok" if source == target else "MISMATCH"
            print(f"verify {table}: mysql={source} postgres={target} {status}")
            if source != target:
                sys.exit(1)

    print("migration complete")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 语法检查**

Run: `python3 -m py_compile deploy/migrate-mysql-to-postgres.py && echo OK`
Expected: `OK`

- [ ] **Step 3: Commit**

```bash
git add deploy/migrate-mysql-to-postgres.py
git commit -m "feat(deploy): add one-off MySQL to PostgreSQL data migration script"
```

---

### Task 13: 线上切换

这一步在集群上操作，不产生代码提交。**执行前先把前面所有任务合并并跑通契约测试。**

- [ ] **Step 1: 在 dev1 的 .env 里补上 POSTGRES_\* 变量**

```bash
ssh dev1
sudo vi /opt/lumora/backend/.env    # 加 POSTGRES_HOST=lumora-postgres.lumora.svc.cluster.local
                                    #    POSTGRES_PORT=5432
                                    #    POSTGRES_DATABASE / USERNAME / PASSWORD（沿用原 MySQL 的库名与账号即可）
                                    # 暂时保留 MYSQL_* 五行，数据搬完再删
sudo chmod 600 /opt/lumora/backend/.env
```

校验：`ssh dev1 'cd /opt/lumora/backend && bash -s' < backend/deploy/check-env.sh`

- [ ] **Step 2: 刷新 Secret 并起 PostgreSQL**

```bash
ssh dev1 "sudo /usr/local/bin/k3s kubectl -n lumora create secret generic lumora-env \
    --from-env-file=/opt/lumora/backend/.env --dry-run=client -o yaml \
  | sudo /usr/local/bin/k3s kubectl apply -f -"
sed 's/__PG_NODE__/dev2/g' deploy/k8s/lumora-postgres.yaml \
  | ssh dev1 "cat > /tmp/pg.yaml && sudo /usr/local/bin/k3s kubectl apply -f /tmp/pg.yaml"
ssh dev1 "sudo /usr/local/bin/k3s kubectl -n lumora rollout status statefulset/lumora-postgres --timeout=180s"
```

- [ ] **Step 3: 停掉后端写入（停机窗口从这里开始）**

```bash
ssh dev1 "sudo /usr/local/bin/k3s kubectl -n lumora scale deployment/lumora-backend-web deployment/lumora-backend-worker deployment/lumora-backend-ops --replicas=0"
```

- [ ] **Step 4: 发布新版本（这一步同时建好 PG 表结构）**

```bash
./deploy/deploy-backend.sh
```

脚本的迁移 Job 会在 PG 上跑 Flyway 建表，apply 清单时 `replicas: 1` 会把上一步缩掉的
Pod 拉回来。**这是预期的**：必须先有分发到 dev2 的新镜像，才能在 PG 上建表。

- [ ] **Step 5: 立刻再缩到 0，并清空刚才那几分钟里写进去的数据**

⚠️ **这一步不能跳过。** 上一步结束后 Pod 已经在接微信回调，会往空的 PG 里写事件，
主键由 IDENTITY 序列从 1 开始发。而下一步导入历史数据时要显式插入原始主键，
两边必然撞在一起。`RESTART IDENTITY` 同时把序列归位。

```bash
ssh dev1 "sudo /usr/local/bin/k3s kubectl -n lumora scale deployment/lumora-backend-web deployment/lumora-backend-worker --replicas=0"
ssh dev1 "sudo /usr/local/bin/k3s kubectl -n lumora exec statefulset/lumora-postgres -- \
  sh -c 'PGPASSWORD=\"\$POSTGRES_PASSWORD\" psql -U \"\$POSTGRES_USER\" -d \"\$POSTGRES_DB\" -c \
  \"TRUNCATE report_delivery_attempt, daily_report, wechat_event RESTART IDENTITY CASCADE;\"'"
```

这几分钟里微信推来的事件会随 TRUNCATE 一起丢弃 —— 微信对未确认的事件会重试，
且 `uq_event_dedup` 保证重复推送不会产生重复行，所以最终不丢数据。

- [ ] **Step 6: 搬数据**

开两个终端各跑一条 port-forward：

```bash
ssh -L 3306:127.0.0.1:3306 dev1 "sudo /usr/local/bin/k3s kubectl -n lumora port-forward svc/lumora-mysql 3306:3306"
ssh -L 5432:127.0.0.1:5432 dev1 "sudo /usr/local/bin/k3s kubectl -n lumora port-forward svc/lumora-postgres 5432:5432"
```

然后（凭据从 dev1 的 .env 里取，不要写进 shell 历史，用 `read -s` 输入）：

```bash
MY_USER=... MY_PASSWORD=... MY_DATABASE=lumora \
PG_USER=... PG_PASSWORD=... PG_DATABASE=lumora \
python3 deploy/migrate-mysql-to-postgres.py
```

Expected: 三张表各输出 `rows copied, sequence reset`，随后三行 `verify ... ok`，最后 `migration complete`

- [ ] **Step 7: 把 Pod 拉回来（停机窗口到此结束）**

```bash
ssh dev1 "sudo /usr/local/bin/k3s kubectl -n lumora scale deployment/lumora-backend-web deployment/lumora-backend-worker --replicas=1"
for d in lumora-backend-web lumora-backend-worker; do
  ssh dev1 "sudo /usr/local/bin/k3s kubectl -n lumora rollout status deployment/$d --timeout=180s"
done
```

- [ ] **Step 8: 验证**

```bash
# worker 就绪标记（它现在同时验证库连通 + 三个定时任务 + 内部发送已开）
ssh dev1 "sudo /usr/local/bin/k3s kubectl -n lumora exec deployment/lumora-backend-worker -- ls -l /tmp/lumora-worker-ready"
# 内部端点在公网仍不可达
curl -s -o /dev/null -w '%{http_code}\n' --resolve lumora.love:443:47.120.54.233 https://lumora.love/internal/reports/2026-01-01/send   # 期望 404
# 手动补发（现在打 worker）
ssh dev1 "sudo /usr/local/bin/k3s kubectl -n lumora exec deployment/lumora-backend-worker -- \
  curl -fsS -X POST http://127.0.0.1:8080/internal/reports/<日期>/send \
  -H 'X-Lumora-Admin-Key: <REPORT_ADMIN_KEY>' -H 'X-Request-Id: <uuid>' -d '{\"regenerate\":true}'"
# 内存
ssh dev1 "sudo /usr/local/bin/k3s kubectl top nodes; sudo /usr/local/bin/k3s kubectl -n lumora top pod"
```

Expected: dev2 内存占用从约 1460 Mi 降到 900 Mi 上下

- [ ] **Step 9: 下线 MySQL**

确认新库跑满一天、日报正常发出后：

```bash
ssh dev1 "sudo /usr/local/bin/k3s kubectl -n lumora delete statefulset lumora-mysql service lumora-mysql"
ssh dev1 "sudo vi /opt/lumora/backend/.env"    # 删掉 MYSQL_* 五行
git rm deploy/k8s/lumora-mysql.yaml
```

`dev2:/opt/lumora/mysql` 目录**保留不动**作为冷备。

---

### Task 14: 文档同步

**Files:**
- Modify: `deploy/README.md`、`README.md`、`backend/README.md`

- [ ] **Step 1: deploy/README.md**

- 「后端发布」的角色表删掉 `ops` 行，`worker` 的「内部发送」列改为「开」
- 「四种角色全部通过 nodeSelector 固定在 dev2」改为「三种角色」（web / worker / migrate）
- 内存预算段按 Task 9 Step 3 的新数字重写
- 数据库一节：`lumora-mysql.yaml` → `lumora-postgres.yaml`，`__MYSQL_NODE__` → `__PG_NODE__`，手动 apply 命令同步
- 「手动补发日报」的 `deployment/lumora-backend-ops` 改成 `deployment/lumora-backend-worker`
- 变更记录加一条 2026-08-09 的条目，写清两次整机 OOM 的证据（被杀进程 RSS 远低于 limit）和这次的两项处置

- [ ] **Step 2: README.md**

「两个子项目」表格里 backend 的技术栈 `MySQL` 改 `PostgreSQL`；「后端跑测试需要 Docker（Testcontainers 会拉起 MySQL）」改成 PostgreSQL。

- [ ] **Step 3: backend/README.md**

「Operating the Database」一节按 PostgreSQL 重写：备份命令从 `mysqldump` 改成 `pg_dump`，数据目录从 `/opt/lumora/mysql` 改成 `/opt/lumora/postgres`。

- [ ] **Step 4: 全仓搜残留**

Run: `grep -rn "MySQL\|mysql\|MYSQL" --include=*.md --include=*.yaml --include=*.sh --include=*.xml --include=*.java . | grep -v "^./docs/superpowers/plans/"`
Expected: 只剩变更记录里对历史事件的叙述

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "docs: describe the PostgreSQL layout and the merged worker role"
```

---

## 风险与已知取舍

| 风险 | 处置 |
|---|---|
| `updated_at` 语义变化：PG 触发器每次 UPDATE 都刷新，MySQL 只在行真变化时刷新 | 已在 V1 注释里写明。租约回收只读该列做超时判断，刷得更勤不影响正确性 |
| worker 变得可从集群内驱动 | Ingress 只放行 `/wechat/callback`，契约测试「公网面」一节继续断言 `/internal/` 公网 404 |
| 无回滚 | 用户明确要求。`dev2:/opt/lumora/mysql` 保留作冷备，真出事只能人工导回 |
| 生成列 `auto_report_id` 在 PG 与 MySQL 的 NULL 唯一性语义 | 两者一致（多个 NULL 互不冲突），已在 V1 注释说明 |
| 停机期间微信回调 5xx | 微信会重试，事件不丢只延迟。窗口预计 5–10 分钟 |
| **主键冲突**：建表必须靠分发到 dev2 的新镜像，而 `deploy-backend.sh` 建完表就把 Pod 拉起来接流量，空库的 IDENTITY 序列从 1 发号，与随后导入的历史主键相撞 | Task 13 Step 5 强制「再缩到 0 + `TRUNCATE ... RESTART IDENTITY CASCADE`」。这一步跳过就会在导数据时报唯一约束冲突 |
